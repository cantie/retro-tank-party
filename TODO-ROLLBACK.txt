TODO
====

MILESTONE 1 (physics):

 - Make sg-physics-2d into its own project
   o API reference
   o Tutorial

MILESTOE 2 (finish porting all the things):

 - Investigate state mismatches:
   * Drop timer being off by one for a frame
     o I think right after a spawn or a pickup?
	 o Also sometimes happens on the first or second tick of the match
	   - Could we be starting the map on the host too early?
 - Bug: Network issues with Elliot
   * Attempt to determine how two clients are connected (relay or not, which relay)
   * Change the "Auto" mode to try non-relayed first, and then relayed after a timeout
   * Experiment with increasing how long messages are "reliable"
   * Experiment with switching from Twilio to custom Coturn? (I wonder if we're just using the wrong regions?)
 - Bug: Crash sometimes when player is killed
 - Bug: Peer with given id already exists
   * Join two players, but don't press Ready
   * Have non-host leave, and then quit game altogether
   * Host should go back (ending match), then start a new one
   * The have non-host connect again
   * Error will happen!
 - Figure out issue where input just stops even though I've never stopped pressing it
   o Easiest to reproduce by pressing a direction on the D-pad
 - Implement a non-debug comparison of state (maybe just sending the hashes of the remote state?)
   o Ideally, passing these hashes would be required and included with other input
     * We'd need a system of ACK'ing hashes, and some contingency if they weren't arriving
   o Clients should count the number of state mismatches in a row - once a threshold is hit, bail

MILESTONE 3 (internal cleanup):

 - Move ping and other RPCs out of SyncManager and into NetworkAdaptor - will hepl with unit tests
 - Convert weapons into nodes? It'd be basically making them the attachment...

BEFORE RELEASE:

 - Implement a way to kick problematic peers, rather than 
 - Smooth out our frame skipping so we don't do it all at once
 - Document and release stuff that was created:
   o Rollback addon
   o Physics engine

BACKLOG:

 - Implement spectators
 - Solve problems packet loss
   o Dig deeper into WebRTC and try to see where things are going awry!
 - Convert concave polygons to a set of convex via triangulation?
   * https://stackoverflow.com/questions/5247994/simple-2d-polygon-triangulation
   * http://gamma.cs.unc.edu/SEIDEL/
   * https://en.wikipedia.org/wiki/Polygon_triangulation
   * Header only library:
     - https://github.com/mapbox/earcut.hpp 
   * Easy to read C library:
     - https://people.sc.fsu.edu/~jburkardt/c_src/polygon_triangulate/polygon_triangulate.html
 - Physics: Have SGArea2D directly build its Array to avoid allocating the intermediate vector
   o OR, pass in a function pointer and use that to build the Array
 - Physics: Implement capsule shape?
 - Setup the debug_rollback_ticks to compare the save states with the original save states so we can know the exact same things happened
 - Profile our rollbacks so we can see how close we are to the frame budget

