TODO
====

MILESTONE 4 (beyond!):

 - Invert the model: make the LogInspector the server, and the game the client
   * It should let us pick which peer we are currently acting as
   * Get the actual player names and session ids into the replay
   * When replay client launches, it should start with the state on the current tick (if we are in the state viewer)
 - Make a FrameViewer version of the replay where it simulates how each frame is actually executed
   * Get debugger working with the replaying version of the game

