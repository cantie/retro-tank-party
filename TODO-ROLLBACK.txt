TODO
====

MILESTONE 1:

 - Solve problems with latency and packet loss
   o Implement a "message serializer" system to allow us to really byte pack our message and reduce size
   o Dig deeper into WebRTC and try to see where things are going awry!
 - See if we can improve rubberbanding problem with better prediction
   o I'd like to try not predicting movement if we've already predicted X frames in a row

INTERMISSION:

 - Decide whether this is a viable way forward or not

MILESTONE 2 (internal cleanup):

 - Profile our rollbacks so we can see how close we are to the frame budget
 - Add some unit tests to verify internal behavior
 - Setup the debug_rollback_ticks to compare the save states with the original save states so we can know the exact same things happened
 - Switch to storing state before starting the next tick? This would ensure that deferred stuff would happen...

MILESTOE 3 (finish porting all the things):

 - Fix Railgun
 - Re-implement abilities
   o Maybe we shouldn't actually create the ability node until we use the ability?
 - Figure out how to handle physics
 - Figure out how to handle sound!
 - Implement spectators

BEFORE RELEASE:

 - Implement a way to kick problematic peers, rather than 
 - Smooth out our frame skipping so we don't do it all at once

