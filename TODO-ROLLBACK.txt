TODO
====

MILESTONE 1 (physics):

 - Attempt to put in game
   o Convert DropCrate and DropCrateSpawnArea
     * The powerup too!
   o Share random seed with all clients at match start
   o ... what next?
 - Unit tests!
   o Reintroduce the physics server to allow testing in GDScript?
 - Code clean-up
   o Break all classes up into their own files?
   o Change the SGFixedVector math functions to take a Variant (so we don't need vector and fixed versions of each method)
   o Rename the fixed_* classes to be camel case?
   o Port over more (all?) of Vector2's methods?
   o Make consistent tabs vs spaces in all source files

MILESTOE 2 (finish porting all the things):

 - Bug: sometimes bullet doesn't explode when hitting DropCrate - it just keeps going (even though crate opens)
 - Fix Railgun
 - Re-implement abilities
   o Maybe we shouldn't actually create the ability node until we use the ability?
 - Search for time Timers in the game and make sure they are switched to NetworkTimer
 - Figure out how to handle physics
 - Figure out how to handle sound!
 - Implement spectators

MILESTONE 3 (internal cleanup):

 - Profile our rollbacks so we can see how close we are to the frame budget
 - Move ping and other RPCs out of SyncManager and into NetworkAdaptor - will hepl with unit tests
 - Add some unit tests to verify internal behavior of SyncManager
 - Setup the debug_rollback_ticks to compare the save states with the original save states so we can know the exact same things happened
 - Switch to storing state before starting the next tick? This would ensure that deferred stuff would happen...

BEFORE RELEASE:

 - Implement a way to kick problematic peers, rather than 
 - Smooth out our frame skipping so we don't do it all at once
 - Document and release stuff that was created:
   o Rollback addon
   o Physics engine

BACKLOG:

 - Solve problems packet loss
   o Dig deeper into WebRTC and try to see where things are going awry!
 - Convert concave polygons to a set of convex via triangulation?
   * https://stackoverflow.com/questions/5247994/simple-2d-polygon-triangulation
   * http://gamma.cs.unc.edu/SEIDEL/
   * https://en.wikipedia.org/wiki/Polygon_triangulation
   * Header only library:
	 - https://github.com/mapbox/earcut.hpp 
   * Easy to read C library:
	 - https://people.sc.fsu.edu/~jburkardt/c_src/polygon_triangulate/polygon_triangulate.html
 - Have SGArea2D directly build its Array to avoid allocating the intermediate vector
   o OR, pass in a function pointer and use that to build the Array

