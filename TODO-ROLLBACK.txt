TODO
====

MILESTONE 0:

 - "Unable to restore state" when player leaves match (disconnects)
 - Setup the debug_rollback_ticks to compare the save states with the original save states so we can know the exact same things happened
 - Implement generic solution for modifying spawned objects after they are spawned
 - Get Battle Royale fully working
 - Switch to storing state before starting the next tick? This would ensure that deferred stuff would happen...

MILESTONE 2:

 - Figure out why a buffer of 20 frames is too small for 100ms of latency (we should be fine up to 320!)
 - How can we ignore the turret position when checking if we need to rollback?
   |-> Ideally, when checking if real input matches our prediction, we wouldn't take turrent position into account
   |-> Maybe we can explicitly put a '_hash' into the dictionary which, if present, is the comparison hash, and we
       check each element of the input dictionary seperately? Can we get the same effect without comparing each?
 - Profile our rollbacks so we can see how close we are to the frame budget

INTERMISSION:

 - Decide whether this is a viable way forward or not

MILESTONE 3:

 - Fix Railgun
 - Re-implement abilities
   o Maybe we shouldn't actually create the ability node until we use the ability?
 - Figure out how to handle physics
 - Figure out how to handle sound!
 - Implement spectators

